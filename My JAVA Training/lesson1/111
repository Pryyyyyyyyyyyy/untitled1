14 - 2

Создать две переменные типа double:
   chislitel = 7.0;
   znamenatel =5.0;
   Используя явный Casting вывести на экран результат приведения 7.0/5.0 к виду правильной дроби.
   То бишь вывести текст следующего типа:
   Целая часть равна - (значение целой части без вещественной).
   Остаток от деления равен - (значение остатка от делеения, в виде целого числа, без вещественной части).
   Знаменатель равен - (значения знаменателя в виде целой части, без вещечтвенной части).

public class Main {
    public static void main(String[] args) {
        double chislitel = 7.0;
        double znamenatel = 5.0;
        int celayaChast = (int) (chislitel / znamenatel);
        int ostatok = (int) (chislitel % znamenatel);
        int znamenatelInt = (int) znamenatel;
        System.out.println("Целая часть равна - " + celayaChast);
        System.out.println("Остаток от деления равен - " + ostatok);
        System.out.println("Знаменатель равен - " + znamenatelInt);
    }
}

В данном примере мы объявляем две переменные типа double - chislitel и znamenatel
- и инициализируем их значениями 7.0 и 5.0 соответственно.
Затем мы используем явное приведение типов (casting) для того, чтобы привести результат деления chislitel на znamenatel к типу int. Для этого мы просто применяем операцию (int) к выражению, содержащему результат деления.
Затем мы вычисляем остаток от деления chislitel на znamenatel, также используя явное приведение типов к int. Наконец, мы выводим результаты в заданном формате с помощью команды System.out.println().



